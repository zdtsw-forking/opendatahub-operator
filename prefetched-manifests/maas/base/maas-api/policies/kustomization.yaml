apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: maas-api-policies

# Auth for maas-api-route is handled by the singleton maas-gateway-auth AuthPolicy
# (managed by maas-controller). The route-level auth-policy.yaml has been removed to
# avoid Kuadrant policy override — gateway defaults now apply to all routes.
resources: []