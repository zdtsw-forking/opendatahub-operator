# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/dscinitialization.opendatahub.io_dscinitializations.yaml
- bases/datasciencecluster.opendatahub.io_datascienceclusters.yaml
- bases/features.opendatahub.io_featuretrackers.yaml
- bases/components.opendatahub.io_dsccodeflares.yaml
- bases/components.opendatahub.io_dscdashboards.yaml
- bases/components.opendatahub.io_dsctos.yaml
- bases/components.opendatahub.io_dsckueues.yaml
- bases/components.opendatahub.io_dsckserves.yaml
- bases/components.opendatahub.io_dscmodelmeshservings.yaml
- bases/components.opendatahub.io_dscmodelregs.yaml
- bases/components.opendatahub.io_dsctrustyais.yaml
- bases/components.opendatahub.io_dscworkbenches.yaml
- bases/components.opendatahub.io_dscrays.yaml
- bases/components.opendatahub.io_dscdatasciencepipelines.yaml
- bases/services.opendatahub.io_dscservices.yaml
#+kubebuilder:scaffold:crdkustomizeresource

# patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_dscinitiatlizations.yaml
#- patches/webhook_in_dscinitializations.yaml
#- patches/webhook_in_datascienceclusters.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_dscinitiatlizations.yaml
#- patches/cainjection_in_dscinitializations.yaml
#- patches/cainjection_in_datascienceclusters.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
